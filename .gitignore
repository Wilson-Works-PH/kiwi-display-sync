# dependencies / build
node_modules
dist
dist-ssr

# logs
logs
*.log
npm-debug.log*

# editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.local

# designer source dump (kept locally, not tracked)
1. PNG-*/

brand
# brand source material (PDF proposal — kept locally, not tracked)
brand/
