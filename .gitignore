# dependencies / build
node_modules
dist
dist-ssr

# logs
logs
*.log
npm-debug.log*

# editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.local

# designer source dump (kept locally, not tracked)
1. PNG-*/

/brand
# brand source material (PDF proposal — kept locally, not tracked)
/brand/
# raw recording takes: kept locally, never deployed (they must stay OUT of public/ —
# Vite copies public/ into dist/ and 29 MB of WebM shipped with the build until 2026-09-08)
recordings/
.secrets/
# client logo source files from the user — curated copies live in src/assets/clients/
/KIWI_CLIENT_LOGO/
